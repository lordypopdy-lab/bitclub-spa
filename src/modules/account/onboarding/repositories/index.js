// Provider switch. Replace with MongoOnboardingRepository later — UI unchanged.
import { LocalStorageOnboardingRepository } from "./LocalStorageOnboardingRepository.js";
export const OnboardingRepository = LocalStorageOnboardingRepository;
